{
  "name": "near-docs-generator",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn clean && tsc && ncc build -o build-ncc -s build-tsc/index.js",
    "clean": "rm -rf build-ncc build-tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.7.14",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.8.2"
  }
}
